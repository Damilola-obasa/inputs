steps:
  - label: "Step 1 - Setup"
    command: "echo 'Setting up environment' && pwd && ls -la"
  
  - label: "Step 2 - Build"
    command: "echo 'Building application' && echo 'Build artifacts ready'"
  
  - label: "Step 3 - Verify"
    command: "echo 'Verifying build output' && echo 'All checks passed'"
