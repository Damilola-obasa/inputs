steps:
  - label: "Builds"
    command: "sleep 65"
    timeout_in_minutes: 1
    notify:
      - slack:
          channels:
            - "#social"
          message: 'failed'
        if: step.outcome == "hard_failed" && build.branch == "main"
