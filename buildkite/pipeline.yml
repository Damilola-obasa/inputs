steps:
  - label: "Generate Name"
    command: |
      buildkite-agent meta-data set "generated-name" "micheal"
    
  - label: "Deploy"
    trigger: "deploy-pipeline"
    build:
      env:
        DEPLOY_TARGET_NAME: $BUILDKITE_META_DATA_generated-name