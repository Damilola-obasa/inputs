steps:
  - label: "Optional test that fails"
    command: "exit 1"
    soft_fail: true
    
  - wait: ~
    continue_on_failure: true
    
  - label: "Main build that passes"
    command: "echo 'Build successful!' && exit 0"


