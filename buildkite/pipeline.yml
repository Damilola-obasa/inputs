steps:
  - label: "Builds"
    command: "sleep 100"
    timeout_in_minutes: 3
    notify:
      - slack:
          channels:
            - "#social"
          message: 'failed'
if: step.outcome == "errored" && build.branch != "main"