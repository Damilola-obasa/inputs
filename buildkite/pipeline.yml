steps:
  - label: "Generate Name"
    command: |
      ./buildkite/gen_name.sh > /tmp/name.txt
      NAME=$(cat /tmp/name.txt)
      buildkite-agent meta-data set "generated-name" "$NAME"
    
  - label: "Deploy"
    trigger: "deploy-pipeline"
    build:
      env:
        DEPLOY_TARGET_NAME: $BUILDKITE_META_DATA_generated-name