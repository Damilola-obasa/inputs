steps:
  - label: "Generate Name"
    command: |
      NAME=$(./buildkite/gen_name.sh)
      echo "Script output: $NAME"
    
  - label: "Deploy"
    trigger: "deploy-pipeline"
    build:
      env:
        DEPLOY_TARGET_NAME: $NAME