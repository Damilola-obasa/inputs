steps:
  - label: "Builds"
    command: "sleep 300"
    timeout_in_minutes: 1
    notify:
    - slack:
        channels:
        - "#social"
        message: 'failed'
      if: step.outcome == "errored" && build.branch != "main"

