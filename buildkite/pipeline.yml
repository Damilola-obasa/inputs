steps:
  - label: "Simulate persistent init failure"
    command: |
      echo "Running job..."
      echo "failed to initialize jobs"
      exit 1
    retry:
      automatic:
        - exit_status: "*"
          limit: 10
