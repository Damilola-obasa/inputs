steps:
  - command: build.sh
    artifact_paths: "build.log"
steps:
  - command: 
buildkite-agent artifact upload “build.log” 
 cat  << EOF | buildkite-agent annotate –style “warning’ –context ‘ctx-error’
    artifact_paths: "build.log"



