steps:
  - label: "🚀 Build"
    command: "echo 'Build completed'"

  - label: "📱 Notify micheal-obasa"
    command: |
      curl -X POST -H 'Content-type: application/json' \
        --data '{"text":"<@micheal-obasa> Job is done!"}' \
        "$SLACK_WEBHOOK_URL"
      echo "Notification sent!"