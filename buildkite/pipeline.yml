steps:
  # Test 3: no-op placeholder — always upload a dummy step with the key
  - label: "Optional step (no-op placeholder)"
    key: "optional-step-real"
    command: echo "Optional step placeholder — always passes"

  - label: "Collector"
    command: echo "Collector ran — dependency satisfied by placeholder"
    depends_on: "optional-step-real"
