steps:
  - label: "Builds"
    command: "nonexistent-command-that-will-cause-error"
    timeout_in_minutes: 1
    notify:
      - slack:
          channels:
            - "#social"
          message: 'failed'
        if: step.outcome == "errored" && build.branch == "main"
