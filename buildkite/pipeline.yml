steps:
  - label: "🚀 Deploy Stage"
    key: "deploy-stage-${BUILDKITE_BUILD_NUMBER}"
    command: |
      echo "Starting deploy stage..."
      echo "This stage will retry on failure and generate another stage"
      echo "❌ Simulating failure to test retry logic"
      exit 1
    retry:
      automatic:
        - exit_status: "*"
          limit: 3