steps:
  - label: "Builds"
    command: "sleep 200"
    timeout_in_minutes: 1
    notify:
      - slack:
          channels:
            - "#social"
          message: 'failed'
        if: build.state == "failed" && build.branch == "main"
