steps:
  - label: "Flaky Step"
    command: |
      echo "Running flaky command..."
      
      # Always fail to trigger retries
      echo "This step always fails!"
      exit 1
    
    retry:
      automatic:
        - exit_status: "*"
          limit: 2