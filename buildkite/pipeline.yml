steps:
  - label: ":pipeline: Build"
    commands:
      - echo "Building..."

  - block: "Manual approval – waiting"
    prompt: "Click to continue after review"
    key: "wait_step"

  - label: ":rocket: Deploy"
    commands:
      - echo "Deploying..."

