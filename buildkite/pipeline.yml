notify:
  - slack: "#social"
    if: build.state == "failed"

steps:
  - key: cancel-me
    soft_fail:
      - exit_status: "*"
    commands: |
      sleep 180
  - commands: |
      sleep 15
      buildkite-agent build cancel
    priority: 100
