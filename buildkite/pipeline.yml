steps:
  - label: "Generate Long Log"
    command: |
      echo "Starting long log generation..."
      
      # Print 1000 numbered lines
      for i in {1..1000}; do
        echo "Log entry $i: This is a test log message with timestamp $(date)"
      done
      
      echo "Long log generation completed!"