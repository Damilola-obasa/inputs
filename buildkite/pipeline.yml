steps:
  # Fast checks run first
  - label: "Go Linter"
    command: "make lint"
  
  - label: "Fast Validation"
    command: "make validate"
  
  # Wait for fast checks - fails pipeline if either failed
  - wait
  
  # All 50 parallel steps using parallelism
  - label: "Integration Tests"
    command: "make test"
    parallelism: 50