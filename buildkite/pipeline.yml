steps:
  - command: echo "Open the gate"
    concurrency_group: gate
    concurrency: 1

  - wait

  - command: echo "This one runs once per build, one build at a time"

  - wait

  - command: echo "Close the gate"
    concurrency_group: gate
    concurrency: 1
