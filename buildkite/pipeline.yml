steps:
  - label: "Simulate persistent init failure"
    command: |
      echo "Running job..."
      echo "failed to initialize jobss"
      exit 1
    retry:
      automatic:
        - exit_status: "*"
          limit: 10
