notify:
  - slack: "#social"
    if: build.state == "failed"

steps:
  - key: cancel-me
    soft_fail:
      - exit_status: -1
    commands: |
      sleep 180
  - commands: |
      sleep 15
      buildkite-agent step cancel --step 'cancel-me'
    priority: 100
