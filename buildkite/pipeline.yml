steps:
  - label: "Generate build.log"
    command: |
      mkdir -p logs
      echo "Build log content" > logs/build.log
  
  - label: "Upload build.log"
    command: buildkite-agent artifact upload log/build.log





