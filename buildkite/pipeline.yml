steps:
  - label: "Build {{matrix.os}}"
    command: |
      make build-{{matrix.os}}
      echo "Build complete for {{matrix.os}}"
    artifact_paths: "dist/{{matrix.os}}/**/*"
    matrix:
      setup:
        os: ["linux", "windows", "macos"]