steps:
  - label: "🚀 Build"
    command: "echo 'Build completed'"

  - label: "📱 Notify Build Passed"
    command: |
      curl -X POST -H 'Content-type: application/json' \
        --data '{"text":"<@micheal-obasa> ✅ Build passed!"}' \
        "$SLACK_WEBHOOK_URL"
      echo "Build passed notification sent!"