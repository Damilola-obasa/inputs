notify:
  - slack: "#social"
    if: build.state == "failed"

steps:
  - label: "Upload job"
    timeout_in_minutes: 1
    soft_fail:
      - exit_status: 42
    commands: |
      trap 'exit 42' TERM
      sleep 180 &
      wait $!
