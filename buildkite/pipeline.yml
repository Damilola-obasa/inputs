steps:
  - label: "Generate artifact"
    key: "generate-artifact"
    command: |
      echo "generating artifact..."
      echo "snapshot data from build $BUILDKITE_BUILD_NUMBER" > snapshot.json
      buildkite-agent artifact upload snapshot.json
      echo "artifact uploaded"
